/**
 * Standardized step identifiers
 * Provides consistent naming for all step types in the system
 */
export enum StepIds {
  CSV_WRITE = "csvWriteStep",
  FILE_READ = "fileReadStep",
  GIT = "gitStep",
  HTTP = "httpStep",
  SHELL = "shellStep",
}
